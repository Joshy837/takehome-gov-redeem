import path from "path";

export const STAFF_MAPPING_FILE = path.join(
  __dirname,
  "../data/staff-id-to-team-mapping-long.csv"
);
export const REDEMPTION_FILE = path.join(
  __dirname,
  "../data/redemption-data.json"
);
